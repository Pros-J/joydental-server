# Railway에서 자동으로 제공되는 PostgreSQL 연결 문자열
DATABASE_URL=postgresql://user:password@host:5432/dbname

# 운영 환경 (Railway 배포 시 production으로 설정)
NODE_ENV=production

# 허용할 프론트엔드 도메인 (CORS) — Railway 배포 URL로 변경
ALLOWED_ORIGIN=*

# 포트 (Railway가 자동 설정하므로 건드릴 필요 없음)
PORT=3000
